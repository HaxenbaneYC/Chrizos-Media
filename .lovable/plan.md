# Improve SEO service visuals and search metadata

## Changes
- Replace the SEO trend chart with three minimal glass icon tiles for Technical Audit, Keyword Strategy, and Content That Ranks, while keeping the Keyword Rankings widget unchanged.
- Give each icon tile a concise supporting line based on the existing SEO service bullets and stack the tiles on narrow screens.
- Add accurate accessible descriptions to both logo images and every CSS/DOM illustration, including the four service graphics, problem funnel, and results board.
- Create a branded 1200×630 social share image from the approved Chrizos Media logo and add matching Open Graph and X image descriptions.
- Add ProfessionalService JSON-LD for Chrizos Media, Dubai, UAE, listing the four services and Instagram profile without inventing a street address.
- Record that the current one-page sitemap is correct and future public pages must be added when created.

## Validation
- Check the SEO card on desktop and mobile, including the three-column-to-stacked layout and unchanged rankings widget.
- Inspect rendered metadata and structured data for the homepage.
- Confirm all meaningful images and illustration graphics have descriptive accessible names and the site builds cleanly.

## Technical details
- Use inline SVG line icons so they remain crisp and inherit the existing semantic color tokens.
- Use the existing glass tokens and brand palette; add only a focused icon-tile style.
- Use `ProfessionalService` with `areaServed` set to Dubai, UAE and the published domain `https://chrizosmedia.com`.
